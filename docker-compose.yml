services:
  app:
    build: .
    ports:
      - "3003:3000"
    environment:
      - JWT_SECRET=${JWT_SECRET}
    volumes:
      - app_data:/app/data
      - app_uploads:/app/uploads
    restart: unless-stopped

volumes:
  app_data:
  app_uploads:
